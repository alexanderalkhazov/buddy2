DEFAULT_TICKER=AAPL

# Optional — richer news coverage for the News/Market News Digest sections.
# Without it, news falls back to yfinance headlines (works fine, fewer articles).
NEWSAPI_KEY=

DB_PATH=storage/buddy.duckdb
